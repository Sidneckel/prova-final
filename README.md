# Projeto FastAPI com Docker e Kubernetes

Este projeto é uma API simples feita com FastAPI, empacotada em Docker e pronta para rodar em Kubernetes.

## Como usar

### Rodar localmente com Docker

1. Buildar a imagem Docker:

```bash
docker build -t sidneckel/hello-fastapi-k8s .
